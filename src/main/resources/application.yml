spring:
  application:
    name: ai-code-mother
server:
  port: 8123
  servlet:
    context-path: /api

springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.aicodemother.controller
knife4j:
  enable: true
  setting:
    language: zh_cn